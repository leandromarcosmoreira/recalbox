diff --git a/Makefile b/Makefile
index c4565d3..198e788 100644
--- a/Makefile
+++ b/Makefile
@@ -67,6 +67,11 @@ ifeq (,$(ARCH))
 	ARCH = $(shell uname -m)
 endif
 
+PAGESIZE16K   := 0
+ifeq ($(PAGESIZE16K), 1)
+	CFLAGS += -DPAGESIZE16K
+endif
+
 # Target Dynarec
 WITH_DYNAREC = $(ARCH)
 
diff --git a/core/stdclass.h b/core/stdclass.h
index 72b30af..9b15dfc 100644
--- a/core/stdclass.h
+++ b/core/stdclass.h
@@ -7,9 +7,13 @@
 #include <rthreads/rthreads.h>
 
 #ifdef _ANDROID
-#include <sys/mman.h>
+  #include <sys/mman.h>
 #else
-#define PAGE_SIZE 4096
+  #ifdef PAGESIZE16K
+    #define PAGE_SIZE 16384
+  #else
+    #define PAGE_SIZE 4096
+  #endif
 #endif
 
 #undef PAGE_MASK
